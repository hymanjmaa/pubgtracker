version: 0.2

phases:
  build:
    commands:
      - virtualenv -p python3 venv
      - source venv/bin/activate
      - pip install -r requirements.txt
      - deactivate
  post_build:
    commands:
      - zip -r pubgtracker_build.zip .
artifacts:
  files:
    - pubgtracker_build.zip